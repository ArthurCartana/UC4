export class Eletronico {
    descricao: string;
    voltagem: string;
  
    constructor(descricao: string, voltagem: string) {
      this.descricao = descricao;
      this.voltagem = voltagem;
    }
  }